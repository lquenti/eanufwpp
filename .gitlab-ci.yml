image: dockerschneider/java-jdk-ant

stages:
    - build
    - dist

compile:
    stage: build
    script:
        - ant
    only:
        - master

doc:
    stage: build
    script:
        - ant doc
    artifacts:
        paths:
            - doc
    only:
        - master
    allow_failure: yes

test:
    stage: build
    script:
        - ant test
    only:
        - master
    allow_failure: yes

dist:
    stage: dist
    script: ant dist
    artifacts:
        paths:
            - dist
    only:
        - master
